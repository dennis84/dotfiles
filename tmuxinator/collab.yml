# ~/.tmuxinator/collab.yml

name: collab
root: ~/projects/collab-web
windows:
  - web:
        layout: main-vertical
        panes:
            - vim
            - gulp
  - backend:
        layout: main-vertical
        panes:
            - cd ~/projects/collab; vim
            - cd ~/projects/collab; cabal run
  - server: http-server
