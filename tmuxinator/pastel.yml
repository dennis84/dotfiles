# ~/.tmuxinator/pastel.yml

project_name: pastel
project_root: ~/projects/pastel
tabs:
  - main:
        layout: even-horizontal
        panes:
            - cd ~/projects/pastel/application && sbt
            - cd ~/projects/pastel && vim
  - shell: cd ~/projects/pastel
