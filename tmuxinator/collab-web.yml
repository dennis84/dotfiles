# ~/.tmuxinator/collab-web.yml

name: collab-web
root: ~/projects/collab-web
windows:
  - editor:
        layout: main-vertical
        panes:
            - vim
            - gulp
  - server: php -S localhost:8000
